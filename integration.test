@@ table devices
    | deviceType    | tags      | size      |
    | Desktop       | desktop   | 1024x768  |

@@ parameterized using devices
Input styles rendered on ${deviceType}
    http://localhost:8089/inputs.html ${size}
        wait 1s
        check test/inputs.gspec --include "${tags}"

@@ parameterized using devices
Font styles rendered on ${deviceType}
    http://localhost:8089/fonts.html ${size}
        wait 1s
        check test/fonts.gspec --include "${tags}"